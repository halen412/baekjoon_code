# 1330 두 수 비교하기
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a, b;
	scanf("%d %d", &a, &b);
	
	if (a > b) {
		printf(">");
	}
	else if (a < b) {
		printf("<");
	}
	else {
		printf("==");
	}

	return 0;
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 9498 시험 성적
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a;
	scanf("%d", &a);
	
	if (a <= 100 && a >= 90) {
		printf("A");
	}
	else if (a <= 89 && a >= 80) {
		printf("B");
	}
	else if (a <= 79 && a >= 70) {
		printf("C");
	}
	else if (a <= 69 && a >= 60) {
		printf("D");
	}
	else {
		printf("F");
	}

	return 0;
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 2753 윤년
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a;
	scanf("%d", &a);
	
	if (a % 4 == 0 && a % 100 != 0) {
		printf("1");
	}
	else if (a % 400 == 0) {
		printf("1");
	}
	else {
		printf("0");
	}

	return 0;
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 14681 사분면 고르기
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a, b;
	scanf("%d\n%d", &a, &b);
	
	if (a > 0 && b > 0) {
		printf("1");
	}
	else if (a < 0 && b > 0) {
		printf("2");
	}
	else if (a < 0 && b < 0) {
		printf("3");
	}
	else {
		printf("4");
	}

	return 0;
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 2884 알람 시계
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int h, m;
	scanf("%d %d", &h, &m);
	
	if (m >= 45) {
		printf("%d %d", h, m - 45);
	}
	else if (h != 0 && m < 45) {
		printf("%d %d", h - 1, 60 - 45 + m);
	}
	else {
		printf("23 %d", 60 - 45 + m);
	}

	return 0;
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 2525 오븐 시계
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int h, m, a;
	scanf("%d %d\n%d", &h, &m, &a);
	m += a;
	while (m >= 60) {
		h += 1;
		m -= 60;
	}
	if (h > 23) {
		printf("%d %d", h - 24, m);
	}
	else {
		printf("%d %d", h, m);
	}

	return 0;
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 2480 주사위 세개
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a, b, c;
	scanf("%d %d %d", &a, &b, &c);

	if (a == b && a == c) {
		printf("%d", 10000 + (a * 1000));
	}
	else if (a == b || a == c){
		printf("%d", 1000 + (a * 100));
	}
	else if (b == c) {
		printf("%d", 1000 + (b * 100));
	}
	else if (a > b && a > c) {
		printf("%d", a * 100);
	}
	else if (b > a && b > c) {
		printf("%d", b * 100);
	}
	else {
		printf("%d", c * 100);
	}

	return 0;
}
